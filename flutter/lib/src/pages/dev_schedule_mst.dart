import 'package:flutter/material.dart';

class DevScheduleMst extends StatelessWidget {
  const DevScheduleMst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Dev Schedule Master')),
    );
  }
}